import { createToggleClient } from "./toggle";

const {
  useVisibility,
  useVisibilityCommands,
  useModelSelector,
  getVisibilityClient,
} = createToggleClient();

export {
  useVisibility,
  useVisibilityCommands,
  useModelSelector,
  getVisibilityClient,
};
